name: Shellcheck
on: [push]
jobs:
  build:
    runs-on: [ubuntu-latest, macOS-latest]
    steps:
    - uses: actions/checkout@v1
    - name: Run shellcheck.
      run: |
        shellcheck pfetch
        sh pfetch
